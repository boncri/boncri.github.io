services:
  app:
    image: nginx:latest
    ports:
      - "80:80"
    volumes:
      - .:/usr/share/nginx/html

  resizer:
    build: .
    volumes:
      - .:/app
    entrypoint: /app/entrypoint_resize.sh
